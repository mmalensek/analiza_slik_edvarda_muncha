#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../resolve/bin/resolve" "$@"
else
  exec node  "$basedir/../../../resolve/bin/resolve" "$@"
fi
# cmd-shim-target=/Users/martinmalensek/Nest/faks/6-semester/osnove_oblikovanja/munch/analiza_slik_edvarda_muncha/web/node_modules/.pnpm/babel-plugin-macros@3.1.0/node_modules/resolve/bin/resolve
